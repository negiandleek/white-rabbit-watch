import is_numeric from "./utils/isNumeric";
import milli_to_time from "./utils/milliToTime";

export default function compose_milli_to_time(ms){
    is_numeric(clock);
    return milli_to_time(clock, start, end);
}